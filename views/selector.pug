extends layout

block content
  h1= title
  - var images = ["Fa.jpg","Dove.jpg","Lux.jpg","margo.jpg","Medimix.jpg"]
  - for(let i=0; i<3;i++){
    -var num = images[Math.floor(Math.random() * 5)]
    img(src= "/images/"+num, alt= num, style={width: "300px",height: "300px"})
  -}
